<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blueocean.azbrain.dao.StatisticsMapper">

  <select id="statArticle" resultType="java.lang.Integer">
    SELECT
      COUNT(*)
    FROM
      azbrain_article
    WHERE
      STATUS = '00'
    <if test="startTime != null">
    AND create_time BETWEEN #{startTime} AND #{endTime}
    </if>
    <if test="startTime == null">
      <![CDATA[ AND create_time < #{endTime} ]]>
    </if>
  </select>

  <select id="statTopic" resultType="java.lang.Integer">
    SELECT
      COUNT(*)
    FROM
      azbrain_topic
    WHERE
      STATUS = '00'
    <if test="startTime != null">
      AND create_time BETWEEN #{startTime} AND #{endTime}
    </if>
    <if test="startTime == null">
      <![CDATA[ AND create_time < #{endTime} ]]>
    </if>
  </select>

  <select id="statConsultation" resultType="java.lang.Integer">
    SELECT
      COUNT(*)
    FROM
      azbrain_user_consultation_log
    WHERE
      STATUS = '00'
    <if test="startTime != null">
      AND create_time BETWEEN #{startTime} AND #{endTime}
    </if>
    <if test="startTime == null">
      <![CDATA[ AND create_time < #{endTime} ]]>
    </if>
  </select>

  <select id="statLikes" resultType="java.lang.Integer">
    SELECT
      COUNT(*)
    FROM
      azbrain_user_likes_article
    WHERE
      1 = 1
    <if test="startTime != null">
      AND like_time BETWEEN #{startTime} AND #{endTime}
    </if>
    <if test="startTime == null">
      <![CDATA[ AND like_time < #{endTime} ]]>
    </if>
  </select>

  <select id="statFollows" resultType="java.lang.Integer">
    SELECT
      COUNT(*)
    FROM
      azbrain_user_follow_topic
    WHERE
      1 = 1
    <if test="startTime != null">
      AND follow_time BETWEEN #{startTime} AND #{endTime}
    </if>
    <if test="startTime == null">
      <![CDATA[ AND follow_time < #{endTime} ]]>
    </if>
  </select>

</mapper>