<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blueocean.azbrain.dao.UserInviteCodeMapper">
  <resultMap id="invite" type="com.blueocean.azbrain.model.UserInviteCode">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="invite_code" jdbcType="VARCHAR" property="inviteCode" />
    <result column="validity" jdbcType="INTEGER" property="validity" />
    <result column="start_time" jdbcType="TIMESTAMP" property="startTime" />
    <result column="receive_user_id" jdbcType="INTEGER" property="receiveUserId" />
    <result column="receive_time" jdbcType="TIMESTAMP" property="receiveTime" />
    <result column="status" jdbcType="CHAR" property="status" />
  </resultMap>
  <sql id="Base_Column_List">
    id, invite_code, validity, start_time, receive_user_id, receive_time, status
  </sql>

  <select id="get" parameterType="java.lang.Integer" resultMap="invite">
    select 
    <include refid="Base_Column_List" />
    from azbrain_user_invite_code
    where id = #{id,jdbcType=INTEGER}
  </select>

  <select id="getByInviteCode" parameterType="java.lang.String" resultMap="invite">
    select
    <include refid="Base_Column_List" />
    from azbrain_user_invite_code
    where invite_code = #{inviteCode,jdbcType=INTEGER}
  </select>

  <insert id="insert" parameterType="com.blueocean.azbrain.model.UserInviteCode" useGeneratedKeys="true" keyProperty="id">
    insert into azbrain_user_invite_code (
      id,
      invite_code,
      validity,
      start_time,
      receive_user_id,
      receive_time,
      status)
    values (
      #{id,jdbcType=INTEGER},
      #{inviteCode,jdbcType=VARCHAR},
      #{validity,jdbcType=INTEGER},
      #{startTime,jdbcType=TIMESTAMP},
      #{receiveUserId,jdbcType=INTEGER},
      #{receiveTime,jdbcType=TIMESTAMP},
      #{status,jdbcType=CHAR})
  </insert>

  <update id="receive">
    UPDATE azbrain_user_invite_code
    SET
      status = '09',
      receive_user_id = #{userId,jdbcType=VARCHAR},
      receive_time = #{receiveTime,jdbcType=TIMESTAMP}
    WHERE
      invite_code = #{inviteCode,jdbcType=INTEGER}
  </update>
</mapper>